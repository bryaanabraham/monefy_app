import 'package:flutter/material.dart';

class PateintInfo extends StatelessWidget {
  const PateintInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}